1、可以手写程序实现，打印输出“Hello World”

   public class HelloWorld{

       public static void main(String[] args){

           System.out.println("Hello World");

       }

   }

* [x] 理解Java语言的跨平台实现原理
* [x] 理解JDK/JRE/JVM的组成和作用
* [x] 能够配置环境变量JAVA_HOME（会参照笔记配置）
* [x] 能够编写HelloWorld程序编译并执行
* [x] 会使用单行注释和多行注释

```flow
st=>start: 页面加载
e=>end: End:>http://www.google.com
op1=>operation: get_hotel_ids|past
op2=>operation: get_proxy|current
sub1=>subroutine: get_proxy|current
op3=>operation: save_comment|current
op4=>operation: set_sentiment|current
op5=>operation: set_record|current

cond1=>condition: ids_remain空?
cond2=>condition: proxy_list空?
cond3=>condition: ids_got空?
cond4=>condition: 爬取成功??
cond5=>condition: ids_remain空?

io1=>inputoutput: ids-remain
io2=>inputoutput: proxy_list
io3=>inputoutput: ids-got

st->op1(right)->io1->cond1
cond1(yes)->sub1->io2->cond2
cond2(no)->op3
cond2(yes)->sub1
cond1(no)->op3->cond4
cond4(yes)->io3->cond3
cond4(no)->io1
cond3(no)->op4
cond3(yes, right)->cond5
cond5(yes)->op5
cond5(no)->cond3
op5->e

```

2、可以说出设置path的目的是什么

   为了使得有关java的命令(包括javac,java,javadoc等)可以在任何文件目录下执行，配置path环境变量后如果首先在当前文件夹下找不到可执行程序，则会环境变量path下添加的文件夹中去寻找，则可实现任何文件夹下都可执行java相关的命令行语句

    JAVA_HOME=bin的上一层目录

    path=%JAVA_HOME%\bin

3、可以说出JDK、JRE和JVM的关系是什么

   它们整体上是包含的关系，JDK包含JRE,而JRE包含JVM；

   JDK=JRE+Java开发工具集（javac.exe，java.exe，javadoc.exe）；

   JRE=JVM+Java核心类库

4、可以说出程序中若只有一个public修饰的类，且此类含main方法，那么类名与源文件名可否不一致

   必须一致，因为一个java源代码文件，在编译后，会去执行public修饰的类中的main方法

5、可以列举Java的注释方式有哪几种，格式为何

   三种：单行注释、多行注释、文档注释

   单行注释：//这个一条单行注释

   多行注释：/*

            多行注释

            */

    文档注释：/**

             @author zcy

             @version v1.0

             */

6、能够清楚地说明变量的使用步骤

   java定义变量的格式：数据类型 变量名 = 变量值;

   注意：1.java中的变量必须先声明，后使用

        2.使用变量名来访问这块区域的值

        3.变量有它的作用域，其定于所在的一对{}代码块内

        4.变量只有在作用域内才有效

7、以下运算符都有哪些具体的符号以及了解其作用：

 a)算术运算符：+(正号)、-(负号)、+、-、*、/、%(取余，结果的符号与被模数的符号相同)、++(不会改变本身的数据类型)、--、+(字符串连接符)

 b)关系运算符：==、!=、<、>、<=、>=、instanceof(检查是否是某个类的对象)

 c)逻辑运算符：&&、||、!、&、！、^

 d)赋值运算符:=、+=、-=、%=、/=、*=

 e)三元运算符：(条件表达式)？ 表达式1：表达式2;

8、能够列举数据类型有哪些:

   基本数据类型：byte,short,int,long,float,double,char,boolean,class,interface,[]

9、能够列举流程控制结构有哪些

   顺序结构

   分支结构：if-else,switch-case

   循环结构：for,while,do-while

10、能实现简单的分支结构

    int num=70;

    if(num>=80){

        System.out.println("成绩优秀！");

    }else if(num>=70&&num<80){

        System.out.println("成绩良好！");

    }else if(num>=60&&num<70){

        System.out.println("成绩合格！");

    }else if(num<60){

        System.out.println("成绩不合格！");

    }